from setuptools import setup, find_packages

setup(
    name='dreamcompute-hello-world',
    description='A sample DreamCompute project',
    author='DreamHost',
    license='MIT',
    packages=find_packages(),
)
